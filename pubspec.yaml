name: maisdata
description: An app to fill forms.


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_modular: ^2.0.1
  hive: ^1.0.0
  hive_flutter: ^0.2.1
  path_provider: ^1.3.0


  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^0.8.2
  build_runner: ^1.11.1

flutter:

  uses-material-design: true

